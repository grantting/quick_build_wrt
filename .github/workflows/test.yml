name: Manual Python Script Test

on:
  # 只允许手动触发
  workflow_dispatch:

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.8 # 使用你所需的 Python 版本

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r build/requirements.txt # 如果有依赖文件，则安装依赖

        pip install -r requirements.txt # 如果有依赖文件，则安装依赖
      
    - name: Run tests
      run: |
        python build/model_selection.py


  test:
    needs: build # 指定此 job 需要等待 'build' job 完成后才能开始
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Test Project
        run: |
          # 这里写测试项目的命令
          echo "Testing the project..."